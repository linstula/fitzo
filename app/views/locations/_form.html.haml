= simple_form_for [trainer_profile, location] do |f|
  = f.input :street_address, placeholder: '123 Easy street'
  = f.input :city
  = f.input :state
  = f.input :zip_code
  = f.submit "Add Location", class: "button small"
